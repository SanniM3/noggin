[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

# Use Netlify's Next.js runtime for API routes and SSR
[[plugins]]
  package = "@netlify/plugin-nextjs"
